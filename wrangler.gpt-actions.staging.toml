name = "vishar-gpt-actions-staging"
main = "workers/gpt-actions-staging.js"
compatibility_date = "2026-08-07"
workers_dev = false

# Retained staging stays fail-closed by default. The OAuth relay may be enabled
# independently for the private GPT OAuth handshake while appointment actions
# remain disabled. The public action API still requires separate edge review
# before GPT_ACTIONS_ENABLED may ever become true.
[vars]
GPT_ACTIONS_ENABLED = "false"
GPT_OAUTH_RELAY_ENABLED = "false"
SUPABASE_URL = "https://gwaliusblwrzisrwnsvs.supabase.co"
