# Native Skill Mirror

This folder mirrors canonical `skills/*/SKILL.md` for runtimes that load project skills from `.agents/skills/`.
The canonical source remains `skills/`; validation fails if mirror files drift.
