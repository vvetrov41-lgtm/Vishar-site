name: RC67 CRM Pages gate cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired temporary helper
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary rc67 helper retired after successful production deployment.'
