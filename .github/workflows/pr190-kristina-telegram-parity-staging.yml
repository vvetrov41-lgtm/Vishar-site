name: PR 190 Kristina Telegram retained staging parity

on:
  pull_request:
    types: [edited]

permissions:
  actions: read
  contents: read
  pull-requests: read

concurrency:
  group: pr190-kristina-telegram-parity-retained-staging
  cancel-in-progress: false

jobs:
  kristina-telegram-parity:
    name: Exact synthetic Kristina Telegram lease, send and acknowledgement
    if: >-
      github.event.action == 'edited' &&
      github.event.sender.login == 'vvetrov41-lgtm' &&
      github.event.pull_request.number == 190 &&
      github.event.pull_request.state == 'open' &&
      github.event.pull_request.draft == true &&
      github.event.pull_request.merged == false &&
      github.event.pull_request.base.ref == 'agent/telegram-outbox-drain' &&
      github.event.pull_request.head.ref == 'agent/kristina-telegram-parity' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(github.event.pull_request.body, format('<!-- RUN_PR190_KRISTINA_TELEGRAM_PARITY:{0} -->', github.event.pull_request.head.sha))
    runs-on: ubuntu-latest
    timeout-minutes: 35
    environment: staging
    env:
      APPROVED_SHA: ${{ github.event.pull_request.head.sha }}
      EXPECTED_BASE_SHA: c5b2d5e2190a10f1708e8c8d234d44778377876c
      EXPECTED_BASE_BRANCH: agent/telegram-outbox-drain
      EXPECTED_HEAD_BRANCH: agent/kristina-telegram-parity
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      STAGING_SUPABASE_URL: ${{ secrets.STAGING_SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      KRISTINA_TELEGRAM_BOT_TOKEN: ${{ secrets.KRISTINA_TELEGRAM_BOT_TOKEN }}
      KRISTINA_TELEGRAM_CHAT_ID: ${{ secrets.KRISTINA_TELEGRAM_CHAT_ID }}
      PRODUCTION_TARGETED: 'false'

    steps:
      - name: Fail closed before checkout
        shell: bash
        run: |
          set -euo pipefail
          [[ "$APPROVED_SHA" =~ ^[0-9a-f]{40}$ ]]
          marker="<!-- RUN_PR190_KRISTINA_TELEGRAM_PARITY:${APPROVED_SHA} -->"
          jq -e \
            --arg sha "$APPROVED_SHA" \
            --arg base_sha "$EXPECTED_BASE_SHA" \
            --arg base "$EXPECTED_BASE_BRANCH" \
            --arg head "$EXPECTED_HEAD_BRANCH" \
            --arg marker "$marker" \
            --arg repo "$GITHUB_REPOSITORY" '
            .action == "edited"
            and .sender.login == "vvetrov41-lgtm"
            and .pull_request.number == 190
            and .pull_request.state == "open"
            and .pull_request.draft == true
            and .pull_request.merged == false
            and .pull_request.base.ref == $base
            and .pull_request.base.sha == $base_sha
            and .pull_request.head.ref == $head
            and .pull_request.head.sha == $sha
            and .pull_request.head.repo.full_name == $repo
            and (.pull_request.body | contains($marker))
          ' "$GITHUB_EVENT_PATH" >/dev/null
          [ "$PRODUCTION_TARGETED" = 'false' ]

      - name: Check out exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.APPROVED_SHA }}
          fetch-depth: 0

      - name: Verify exact PR, stack base and successful normal CI
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          [ "$(git rev-parse HEAD)" = "$APPROVED_SHA" ]
          [ "$(git rev-parse "${EXPECTED_BASE_SHA}^{commit}")" = "$EXPECTED_BASE_SHA" ]
          git merge-base --is-ancestor "$EXPECTED_BASE_SHA" "$APPROVED_SHA"

          gh api "repos/$GITHUB_REPOSITORY/pulls/189" > "$RUNNER_TEMP/pr189.json"
          gh api "repos/$GITHUB_REPOSITORY/pulls/190" > "$RUNNER_TEMP/pr190.json"
          jq -e \
            --arg sha "$EXPECTED_BASE_SHA" \
            '.state == "open" and .draft == true and .merged == false
             and .head.ref == "agent/telegram-outbox-drain" and .head.sha == $sha' \
            "$RUNNER_TEMP/pr189.json" >/dev/null
          jq -e \
            --arg sha "$APPROVED_SHA" \
            --arg base_sha "$EXPECTED_BASE_SHA" \
            --arg base "$EXPECTED_BASE_BRANCH" \
            --arg head "$EXPECTED_HEAD_BRANCH" \
            --arg repo "$GITHUB_REPOSITORY" '
            .state == "open" and .draft == true and .merged == false
            and .base.ref == $base and .base.sha == $base_sha
            and .head.ref == $head and .head.sha == $sha
            and .head.repo.full_name == $repo
          ' "$RUNNER_TEMP/pr190.json" >/dev/null

          mkdir -p "$RUNNER_TEMP/kristina-telegram-parity-evidence"
          gh api --paginate "repos/$GITHUB_REPOSITORY/actions/runs?head_sha=$APPROVED_SHA&per_page=100" \
            --jq '.workflow_runs[] | {id,name,event,status,conclusion,head_sha,html_url}' \
            | jq -s . > "$RUNNER_TEMP/runs.json"
          jq -n --slurpfile runs "$RUNNER_TEMP/runs.json" --arg sha "$APPROVED_SHA" '
            ["Static Validation", "CRM and booking validation"] as $required
            | reduce $required[] as $name ({};
                ($runs[0] | map(select(
                  .name == $name and .event == "pull_request" and .head_sha == $sha
                  and .status == "completed" and .conclusion == "success"
                )) | sort_by(.id) | last) as $run
                | if $run == null then error("missing exact-head normal CI: " + $name)
                  else .[$name] = $run end
              )
          ' > "$RUNNER_TEMP/normal-ci-runs.json"
          jq -r 'to_entries[].value.id' "$RUNNER_TEMP/normal-ci-runs.json" | while read -r run_id; do
            gh api "repos/$GITHUB_REPOSITORY/actions/runs/${run_id}/jobs?per_page=100" \
              --jq '{run_id:'"$run_id"',jobs:[.jobs[]|{id,name,status,conclusion}]}'
          done | jq -s . > "$RUNNER_TEMP/normal-ci-jobs.json"
          jq -n \
            --argjson runs "$(cat "$RUNNER_TEMP/normal-ci-runs.json")" \
            --argjson jobs "$(cat "$RUNNER_TEMP/normal-ci-jobs.json")" \
            '{runs:$runs,jobs:$jobs}' \
            > "$RUNNER_TEMP/kristina-telegram-parity-evidence/normal-ci.json"

      - name: Verify encrypted Kristina staging configuration
        shell: bash
        run: |
          set -euo pipefail
          missing=()
          for name in SUPABASE_ACCESS_TOKEN STAGING_SUPABASE_URL SUPABASE_SECRET_KEY \
            KRISTINA_TELEGRAM_BOT_TOKEN KRISTINA_TELEGRAM_CHAT_ID; do
            [ -n "${!name:-}" ] || missing+=("$name")
          done
          [ "${#missing[@]}" -eq 0 ] || {
            printf 'Missing encrypted staging configuration names: %s\n' "$(IFS=,; echo "${missing[*]}")" >&2
            exit 1
          }
          [ "$STAGING_SUPABASE_URL" = 'https://gwaliusblwrzisrwnsvs.supabase.co' ]
          case "$SUPABASE_SECRET_KEY" in sb_secret_*) ;; *) exit 1 ;; esac
          [ "$PRODUCTION_TARGETED" = 'false' ]

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install exact locked dependencies
        run: npm ci

      - name: Recheck exact-head code before hosted mutation
        shell: bash
        run: |
          set -euo pipefail
          bash -n scripts/kristina-telegram-parity-staging-e2e.sh
          node --check scripts/run-kristina-telegram-parity.mjs
          npm run test:worker
          npm run scan:secrets
          npm run check:worker-bundle

      - name: Provision one route and run one exact synthetic Kristina retry
        shell: bash
        run: bash scripts/kristina-telegram-parity-staging-e2e.sh

      - name: Upload safe Kristina parity evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr190-kristina-telegram-parity-${{ env.APPROVED_SHA }}
          path: ${{ runner.temp }}/kristina-telegram-parity-evidence
          if-no-files-found: warn
          retention-days: 30
