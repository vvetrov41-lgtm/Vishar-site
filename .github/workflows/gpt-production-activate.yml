name: GPT production action activation

on:
  pull_request:
    types: [edited]

permissions:
  actions: read
  contents: read
  pull-requests: read

concurrency:
  group: gpt-production-activate
  cancel-in-progress: false

jobs:
  activate:
    if: >-
      github.event.pull_request.draft == true &&
      github.event.pull_request.head.ref == 'agent/gpt-production-actions' &&
      github.event.pull_request.base.ref == 'release/private-crm-rc25-whatsapp' &&
      contains(github.event.pull_request.body, '<!-- ENABLE_GPT_PRODUCTION_ACTIONS -->')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: crm-production
    env:
      APPROVED_SHA: ${{ github.event.pull_request.head.sha }}
      SUPABASE_URL: ${{ vars.CRM_PRODUCTION_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.CRM_PRODUCTION_SUPABASE_PUBLISHABLE_KEY }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CRM_PRODUCTION_CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CRM_PRODUCTION_CLOUDFLARE_ACCOUNT_ID }}
      WORKER_NAME: vishar-gpt-actions-production
      TARGET_HOST: gpt-actions.vishartattoo.com

    steps:
      - name: Fail-closed PR guard
        shell: bash
        run: |
          set -euo pipefail
          jq -e --arg sha "$APPROVED_SHA" --arg repo "$GITHUB_REPOSITORY" '
            .action == "edited"
            and .sender.login == "vvetrov41-lgtm"
            and .pull_request.state == "open"
            and .pull_request.draft == true
            and .pull_request.merged == false
            and .pull_request.base.ref == "release/private-crm-rc25-whatsapp"
            and .pull_request.head.ref == "agent/gpt-production-actions"
            and .pull_request.head.sha == $sha
            and .pull_request.head.repo.full_name == $repo
            and (.pull_request.body | contains("<!-- ENABLE_GPT_PRODUCTION_ACTIONS -->"))
          ' "$GITHUB_EVENT_PATH" >/dev/null
          [ "$SUPABASE_URL" = 'https://vfjexhfdbrjmuxfdvbdx.supabase.co' ]
          case "$SUPABASE_PUBLISHABLE_KEY" in sb_publishable_*) ;; *) exit 1 ;; esac
          [ -n "$CLOUDFLARE_API_TOKEN" ]
          [ -n "$CLOUDFLARE_ACCOUNT_ID" ]

      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.APPROVED_SHA }}

      - name: Verify exact head and normal CI
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$APPROVED_SHA"
          test -z "$(git status --short)"
          gh api --paginate "repos/$GITHUB_REPOSITORY/actions/runs?head_sha=$APPROVED_SHA&per_page=100" --jq '.workflow_runs[] | {name,status,conclusion,head_sha}' | jq -s . > "$RUNNER_TEMP/runs.json"
          for workflow in 'Static Validation' 'CRM and booking validation'; do
            jq -e --arg name "$workflow" --arg sha "$APPROVED_SHA" 'any(.[]; .name == $name and .head_sha == $sha and .status == "completed" and .conclusion == "success")' "$RUNNER_TEMP/runs.json" >/dev/null
          done

      - name: Setup Node and revalidate production boundary
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Test exact production action runtime
        run: |
          npm ci
          npm audit --audit-level=moderate
          node scripts/test-gpt-production.mjs
          node scripts/test-gpt-production-config.mjs
          npm run validate:gpt-actions
          npm run check:gpt-production-bundle
          npm run scan:secrets

      - name: Deploy action-enabled production GPT edge
        shell: bash
        run: |
          set -euo pipefail
          npx wrangler deploy --config wrangler.gpt-actions.production.toml --name "$WORKER_NAME" --strict --var SUPABASE_URL:"$SUPABASE_URL" --var SUPABASE_PUBLISHABLE_KEY:"$SUPABASE_PUBLISHABLE_KEY" --var VISHAR_ENVIRONMENT:production --var GPT_OAUTH_RELAY_ENABLED:true --var GPT_ACTIONS_ENABLED:true

      - name: Verify edge is OAuth-gated after activation
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 20); do
            privacy="$(curl -sS -o /dev/null -w '%{http_code}' --max-time 10 "https://$TARGET_HOST/privacy" || true)"
            oauth="$(curl -sS -o /dev/null -w '%{http_code}' --max-time 10 "https://$TARGET_HOST/oauth/authorize" || true)"
            actions="$(curl -sS -o /dev/null -w '%{http_code}' --max-time 10 "https://$TARGET_HOST/v1/appointments?from=2026-08-01T00%3A00%3A00Z&to=2026-09-01T00%3A00%3A00Z" || true)"
            if [ "$privacy" = 200 ] && [ "$oauth" = 400 ] && [ "$actions" = 401 ]; then exit 0; fi
            sleep 6
          done
          echo 'Production GPT edge did not reach expected OAuth-gated action state.' >&2
          exit 1

      - name: Boundary summary
        if: always()
        shell: bash
        run: |
          {
            echo '### GPT production action activation'
            echo "- Exact SHA: \`$APPROVED_SHA\`"
            echo '- Worker host: `gpt-actions.vishartattoo.com`'
            echo '- OAuth relay: enabled'
            echo '- Appointment actions: enabled but still require valid Supabase OAuth token and database artist binding'
            echo '- Database migrations: none'
            echo '- GPT client binding mutation: none'
            echo '- Retained staging targeted: no'
          } >> "$GITHUB_STEP_SUMMARY"
