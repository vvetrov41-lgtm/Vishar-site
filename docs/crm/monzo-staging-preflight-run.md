# Monzo retained staging preflight run

This validation-only document exists solely to give the retained-staging preflight operator an exact child PR head.

It contains no credentials, provider identifiers, deployment configuration or runtime behavior. The associated operator is read-only and must not deploy, mutate Supabase, create Cloudflare resources, call Monzo, register a webhook or target production.
